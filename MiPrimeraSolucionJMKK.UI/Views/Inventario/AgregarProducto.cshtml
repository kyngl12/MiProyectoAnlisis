@model MiPrimeraSolucionJMKK.Abstracciones.Modelos.Productos.ProductoDto

@{
    ViewBag.Title = "Agregar Producto";
}

<style>
    :root {
        --product-blue-950: #06142f;
        --product-blue-900: #0a1f46;
        --product-blue-800: #10376b;
        --product-blue-700: #185695;
        --product-blue-600: #2878bd;
        --product-blue-500: #4298d4;
        --product-blue-400: #68a5d8;
        --product-blue-300: #9acced;
        --product-blue-200: #c9e5f6;
        --product-blue-100: #e7f3fb;
        --product-blue-50: #f5faff;
        --product-white: #ffffff;
        --product-text: #14263a;
        --product-muted: #718397;
        --product-border: #dce9f3;
        --product-border-light: #eaf1f7;
        --product-shadow-sm: 0 7px 22px rgba(6, 20, 47, .08);
        --product-shadow-md: 0 18px 42px rgba(6, 20, 47, .12);
        --product-shadow-lg: 0 28px 65px rgba(6, 20, 47, .16);
    }

    .product-form-page {
        width: 100%;
        padding: 18px 0 35px;
    }

    .product-form-hero {
        position: relative;
        min-height: 230px;
        padding: 42px 46px;
        display: flex;
        align-items: flex-end;
        border-radius: 24px;
        background: radial-gradient(circle at 85% 20%, rgba(104, 165, 216, .25), transparent 30%), radial-gradient(circle at 15% 90%, rgba(40, 120, 189, .18), transparent 32%), linear-gradient(135deg, var(--product-blue-950), var(--product-blue-800));
        box-shadow: var(--product-shadow-lg);
        overflow: hidden;
    }

        .product-form-hero::before {
            content: "";
            position: absolute;
            width: 330px;
            height: 330px;
            right: -140px;
            top: -160px;
            border: 54px solid rgba(255, 255, 255, .035);
            border-radius: 50%;
        }

        .product-form-hero::after {
            content: "PRODUCTO";
            position: absolute;
            right: 30px;
            bottom: -12px;
            color: rgba(255, 255, 255, .035);
            font-size: clamp(4rem, 8vw, 8rem);
            font-weight: 800;
            letter-spacing: -.08em;
            line-height: .8;
            white-space: nowrap;
            pointer-events: none;
            user-select: none;
        }

    .product-form-heading {
        position: relative;
        z-index: 2;
        max-width: 700px;
    }

    .product-form-eyebrow {
        display: inline-block;
        margin-bottom: 15px;
        padding-bottom: 7px;
        border-bottom: 2px solid var(--product-blue-400);
        color: var(--product-blue-100);
        font-size: .7rem;
        font-weight: 800;
        letter-spacing: .16em;
        text-transform: uppercase;
    }

    .product-form-heading h1 {
        margin: 0 0 13px;
        color: var(--product-white) !important;
        font-size: clamp(2.3rem, 4vw, 4.1rem);
        font-weight: 800;
        line-height: 1.03;
        letter-spacing: -.055em;
    }

    .product-form-heading p {
        margin: 0;
        color: rgba(255, 255, 255, .74);
        font-size: 1rem;
        line-height: 1.7;
    }

    .product-form-alerts {
        margin-top: 24px;
    }

        .product-form-alerts .alert {
            margin-bottom: 12px;
            padding: 14px 17px;
            border-radius: 12px;
            font-size: .86rem;
            font-weight: 600;
            box-shadow: 0 5px 14px rgba(6, 20, 47, .05);
        }

        .product-form-alerts .alert-success {
            color: #1f6942;
            background: #effaf4;
            border-color: #bfe7cf;
        }

        .product-form-alerts .alert-danger {
            color: #842029;
            background: #fff2f3;
            border-color: #f3c5ca;
        }

    .product-form-shell {
        margin-top: 25px;
        display: grid;
        grid-template-columns: minmax(0, 1.3fr) minmax(260px, .7fr);
        gap: 24px;
    }

    .product-form-panel {
        padding: 32px;
        border: 1px solid var(--product-border);
        border-radius: 20px;
        background: linear-gradient( 180deg, var(--product-white), var(--product-blue-50) );
        box-shadow: var(--product-shadow-md);
    }

    .product-form-panel-header {
        margin-bottom: 28px;
        padding-bottom: 18px;
        border-bottom: 1px solid var(--product-border-light);
    }

        .product-form-panel-header h2 {
            margin: 0 0 7px;
            color: var(--product-blue-950);
            font-size: 1.4rem;
            font-weight: 800;
        }

        .product-form-panel-header p {
            margin: 0;
            color: var(--product-muted);
            font-size: .86rem;
            line-height: 1.6;
        }

    .product-field {
        margin-bottom: 20px;
    }

        .product-field label {
            display: block;
            margin-bottom: 8px;
            color: var(--product-text);
            font-size: .82rem;
            font-weight: 700;
        }

        .product-field .form-control,
        .product-field select.form-control {
            min-height: 50px;
            padding: 12px 15px;
            color: var(--product-text);
            background: var(--product-white);
            border: 1px solid var(--product-border);
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(6, 20, 47, .04);
            transition: border-color .16s ease, box-shadow .16s ease;
        }

            .product-field .form-control:hover,
            .product-field select.form-control:hover {
                border-color: var(--product-blue-300);
            }

            .product-field .form-control:focus,
            .product-field select.form-control:focus {
                border-color: var(--product-blue-500);
                box-shadow: 0 0 0 4px rgba(66, 152, 212, .12);
            }

        .product-field .text-danger {
            display: block;
            margin-top: 6px;
            font-size: .78rem;
        }

    .product-validation {
        margin-bottom: 18px;
        padding: 12px 14px;
        border: 1px solid #f3c5ca;
        border-radius: 10px;
        color: #842029;
        background: #fff2f3;
        font-size: .82rem;
    }

        .product-validation ul {
            margin: 0;
            padding-left: 18px;
        }

    .product-form-submit {
        min-height: 52px;
        padding: 13px 24px;
        border: 1px solid var(--product-blue-600) !important;
        border-radius: 10px !important;
        color: var(--product-white) !important;
        background: linear-gradient( 135deg, var(--product-blue-600), var(--product-blue-700) ) !important;
        font-size: .88rem;
        font-weight: 800;
        letter-spacing: .02em;
        box-shadow: 0 9px 22px rgba(40, 120, 189, .22);
        transition: transform .16s ease, box-shadow .16s ease;
    }

        .product-form-submit:hover {
            color: var(--product-white) !important;
            background: linear-gradient( 135deg, var(--product-blue-700), var(--product-blue-800) ) !important;
            box-shadow: 0 13px 28px rgba(40, 120, 189, .28);
            transform: translateY(-2px);
        }

    .product-form-aside {
        padding: 30px;
        border: 1px solid var(--product-border);
        border-radius: 20px;
        background: radial-gradient(circle at top right, rgba(104, 165, 216, .13), transparent 35%), var(--product-white);
        box-shadow: var(--product-shadow-sm);
    }

    .product-form-aside-number {
        margin-bottom: 20px;
        color: var(--product-blue-100);
        font-size: 5.2rem;
        font-weight: 800;
        letter-spacing: -.08em;
        line-height: .85;
    }

    .product-form-aside h3 {
        margin-bottom: 12px;
        color: var(--product-blue-950);
        font-size: 1.2rem;
        font-weight: 800;
    }

    .product-form-aside p {
        margin-bottom: 0;
        color: var(--product-muted);
        font-size: .88rem;
        line-height: 1.7;
    }

    @@media (max-width: 991.98px) {
        .product-form-shell {
            grid-template-columns: 1fr;
        }

        .product-form-aside {
            order: -1;
        }
    }

    @@media (max-width: 575.98px) {
        .product-form-hero {
            min-height: 290px;
            padding: 30px 22px;
            border-radius: 18px;
        }

        .product-form-heading h1 {
            font-size: 2.4rem;
        }

        .product-form-panel,
        .product-form-aside {
            padding: 24px 20px;
            border-radius: 16px;
        }

        .product-form-submit {
            width: 100%;
        }
    }
</style>

<div class="product-form-page">

    <section class="product-form-hero">

        <div class="product-form-heading">

            <span class="product-form-eyebrow">
                Gestión de inventario
            </span>

            <h1>
                Registrar producto
            </h1>

            <p>
                Agrega un nuevo producto al inventario y define su categoría,
                cantidad inicial y precio unitario.
            </p>

        </div>

    </section>

    <div class="product-form-alerts">

        @if (TempData["MensajeError"] != null)
        {
            <div class="alert alert-danger">
                @TempData["MensajeError"]
            </div>
        }

        @if (TempData["MensajeExito"] != null)
        {
            <div class="alert alert-success">
                @TempData["MensajeExito"]
            </div>
        }

    </div>

    <div class="product-form-shell">

        <section class="product-form-panel">

            <div class="product-form-panel-header">

                <h2>
                    Información del producto
                </h2>

                <p>
                    Complete los campos necesarios para registrar el producto.
                </p>

            </div>

            @using (Html.BeginForm(
                "AgregarProducto",
                "Inventario",
                FormMethod.Post))
            {
                @Html.AntiForgeryToken()

                @Html.ValidationSummary(
                    true,
                    "Por favor corrija los errores del formulario",
                    new
                    {
                        @class = "product-validation"
                    })

                @* Codigo se genera automatico en el servidor *@

                <div class="product-field">

                    @Html.LabelFor(m => m.Nombre)

                    @Html.TextBoxFor(
                        m => m.Nombre,
                        new
                        {
                            @class = "form-control"
                        })

                    @Html.ValidationMessageFor(
                        m => m.Nombre,
                        "",
                        new
                        {
                            @class = "text-danger"
                        })

                </div>

                <div class="product-field">

                    @Html.LabelFor(m => m.Categoria)

                    @Html.DropDownListFor(
                        m => m.Categoria,
                        ViewBag.Categorias as System.Web.Mvc.SelectList,
                        "-- Seleccione --",
                        new
                        {
                            @class = "form-control"
                        })

                    @Html.ValidationMessageFor(
                        m => m.Categoria,
                        "",
                        new
                        {
                            @class = "text-danger"
                        })

                </div>

                <div class="product-field">

                    @Html.LabelFor(m => m.Cantidad)

                    @Html.TextBoxFor(
                        m => m.Cantidad,
                        new
                        {
                            @class = "form-control",
                            type = "number",
                            step = "1"
                        })

                    @Html.ValidationMessageFor(
                        m => m.Cantidad,
                        "",
                        new
                        {
                            @class = "text-danger"
                        })

                </div>

                <div class="product-field">

                    @Html.LabelFor(m => m.PrecioUnitario)

                    @Html.TextBoxFor(
                        m => m.PrecioUnitario,
                        new
                        {
                            @class = "form-control",
                            type = "number",
                            step = "0.01"
                        })

                    @Html.ValidationMessageFor(
                        m => m.PrecioUnitario,
                        "",
                        new
                        {
                            @class = "text-danger"
                        })

                </div>

                <button class="btn btn-primary product-form-submit"
                        type="submit">
                    Guardar producto
                </button>
            }

        </section>

        <aside class="product-form-aside">

            <div class="product-form-aside-number">
                01
            </div>

            <h3>
                Registro claro y ordenado
            </h3>

            <p>
                El código se genera automáticamente en el servidor.
                Solo debes completar la información comercial y de inventario.
            </p>

        </aside>

    </div>

</div>