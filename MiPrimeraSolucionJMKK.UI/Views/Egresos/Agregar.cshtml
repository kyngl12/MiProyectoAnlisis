@model MiPrimeraSolucionJMKK.Abstracciones.Modelos.Contabilidad.EgresoDto
@using System.Collections.Generic

@{
    ViewBag.Title = "Registrar Gasto";
}

<style>
    :root {
        --expense-form-blue-950: #06142f;
        --expense-form-blue-900: #0a1f46;
        --expense-form-blue-800: #10376b;
        --expense-form-blue-700: #185695;
        --expense-form-blue-600: #2878bd;
        --expense-form-blue-500: #4298d4;
        --expense-form-blue-400: #68a5d8;
        --expense-form-blue-300: #9acced;
        --expense-form-blue-200: #c9e5f6;
        --expense-form-blue-100: #e7f3fb;
        --expense-form-blue-50: #f5faff;
        --expense-form-white: #ffffff;
        --expense-form-text: #14263a;
        --expense-form-muted: #718397;
        --expense-form-border: #dce9f3;
        --expense-form-border-light: #eaf1f7;
        --expense-form-shadow-sm: 0 7px 22px rgba(6, 20, 47, .08);
        --expense-form-shadow-md: 0 18px 42px rgba(6, 20, 47, .12);
        --expense-form-shadow-lg: 0 28px 65px rgba(6, 20, 47, .16);
    }

    .expense-form-page {
        width: 100%;
        padding: 18px 0 35px;
    }

    /* =====================================================
       ENCABEZADO
       ===================================================== */

    .expense-form-header {
        position: relative;
        min-height: 245px;
        padding: 42px 46px;
        display: flex;
        align-items: flex-end;
        border-radius: 24px;
        background: radial-gradient( circle at 85% 20%, rgba(104, 165, 216, .25), transparent 30% ), radial-gradient( circle at 15% 90%, rgba(40, 120, 189, .20), transparent 32% ), linear-gradient( 135deg, var(--expense-form-blue-950), var(--expense-form-blue-800) );
        box-shadow: var(--expense-form-shadow-lg);
        overflow: hidden;
    }

        .expense-form-header::before {
            content: "";
            position: absolute;
            width: 330px;
            height: 330px;
            right: -140px;
            top: -160px;
            border: 54px solid rgba(255, 255, 255, .035);
            border-radius: 50%;
        }

        .expense-form-header::after {
            content: "GASTO";
            position: absolute;
            right: 30px;
            bottom: -12px;
            color: rgba(255, 255, 255, .035);
            font-size: clamp(4rem, 8vw, 8rem);
            font-weight: 800;
            letter-spacing: -.08em;
            line-height: .8;
            white-space: nowrap;
            pointer-events: none;
            user-select: none;
        }

    .expense-form-heading {
        position: relative;
        z-index: 2;
        max-width: 720px;
    }

    .expense-form-eyebrow {
        display: inline-block;
        margin-bottom: 15px;
        padding-bottom: 7px;
        border-bottom: 2px solid var(--expense-form-blue-400);
        color: var(--expense-form-blue-100);
        font-size: .7rem;
        font-weight: 800;
        letter-spacing: .16em;
        text-transform: uppercase;
    }

    .expense-form-heading h1 {
        margin: 0 0 13px;
        color: var(--expense-form-white) !important;
        font-size: clamp(2.3rem, 4vw, 4.2rem);
        font-weight: 800;
        line-height: 1.03;
        letter-spacing: -.055em;
    }

    .expense-form-heading p {
        max-width: 650px;
        margin: 0;
        color: rgba(255, 255, 255, .74);
        font-size: 1rem;
        line-height: 1.7;
    }

    /* =====================================================
       ALERTAS
       ===================================================== */

    .expense-form-alerts {
        margin-top: 25px;
    }

        .expense-form-alerts .alert {
            margin-bottom: 12px;
            padding: 14px 17px;
            border-radius: 12px;
            font-size: .86rem;
            font-weight: 600;
            box-shadow: 0 5px 14px rgba(6, 20, 47, .05);
        }

        .expense-form-alerts .alert-danger {
            color: #842029;
            background: #fff2f3;
            border-color: #f3c5ca;
        }

    /* =====================================================
       DISTRIBUCIÓN
       ===================================================== */

    .expense-form-shell {
        margin-top: 25px;
        display: grid;
        grid-template-columns: minmax(0, 1.35fr) minmax(260px, .65fr);
        gap: 24px;
    }

    .expense-form-panel {
        padding: 32px;
        border: 1px solid var(--expense-form-border);
        border-radius: 20px;
        background: linear-gradient( 180deg, var(--expense-form-white), var(--expense-form-blue-50) );
        box-shadow: var(--expense-form-shadow-md);
    }

    .expense-form-panel-header {
        margin-bottom: 28px;
        padding-bottom: 18px;
        border-bottom: 1px solid var(--expense-form-border-light);
    }

        .expense-form-panel-header h2 {
            margin: 0 0 7px;
            color: var(--expense-form-blue-950);
            font-size: 1.4rem;
            font-weight: 800;
        }

        .expense-form-panel-header p {
            margin: 0;
            color: var(--expense-form-muted);
            font-size: .86rem;
            line-height: 1.6;
        }

    /* =====================================================
       CAMPOS
       ===================================================== */

    .expense-form-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 18px;
    }

    .expense-form-field {
        margin-bottom: 18px;
    }

        .expense-form-field.full-width {
            grid-column: 1 / -1;
        }

        .expense-form-field label {
            display: block;
            margin-bottom: 8px;
            color: var(--expense-form-text);
            font-size: .82rem;
            font-weight: 700;
        }

        .expense-form-field .form-control,
        .expense-form-field .form-select {
            min-height: 50px;
            padding: 12px 15px;
            color: var(--expense-form-text);
            background: var(--expense-form-white);
            border: 1px solid var(--expense-form-border);
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(6, 20, 47, .04);
            transition: border-color .16s ease, box-shadow .16s ease;
        }

        .expense-form-field textarea.form-control {
            min-height: 125px;
            resize: vertical;
        }

        .expense-form-field .form-control:hover,
        .expense-form-field .form-select:hover {
            border-color: var(--expense-form-blue-300);
        }

        .expense-form-field .form-control:focus,
        .expense-form-field .form-select:focus {
            border-color: var(--expense-form-blue-500);
            box-shadow: 0 0 0 4px rgba(66, 152, 212, .12);
        }

        .expense-form-field .form-control::placeholder {
            color: #97a9b8;
        }

        .expense-form-field .text-danger {
            display: block;
            margin-top: 6px;
            font-size: .78rem;
        }

    /* =====================================================
       BOTONES
       ===================================================== */

    .expense-form-actions {
        margin-top: 8px;
        display: flex;
        justify-content: flex-end;
        flex-wrap: wrap;
        gap: 12px;
    }

    .expense-form-submit,
    .expense-form-cancel {
        min-height: 50px;
        padding: 12px 22px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border-radius: 10px !important;
        font-size: .86rem;
        font-weight: 800;
        text-decoration: none !important;
        transition: transform .16s ease, background-color .16s ease, border-color .16s ease, box-shadow .16s ease;
    }

    .expense-form-submit {
        color: var(--expense-form-white) !important;
        background: linear-gradient( 135deg, var(--expense-form-blue-600), var(--expense-form-blue-700) ) !important;
        border: 1px solid var(--expense-form-blue-600) !important;
        box-shadow: 0 9px 22px rgba(40, 120, 189, .22);
    }

        .expense-form-submit:hover {
            color: var(--expense-form-white) !important;
            background: linear-gradient( 135deg, var(--expense-form-blue-700), var(--expense-form-blue-800) ) !important;
            transform: translateY(-2px);
        }

    .expense-form-cancel {
        color: var(--expense-form-blue-700) !important;
        background: var(--expense-form-white) !important;
        border: 1px solid var(--expense-form-blue-200) !important;
    }

        .expense-form-cancel:hover {
            color: var(--expense-form-white) !important;
            background: var(--expense-form-blue-700) !important;
            border-color: var(--expense-form-blue-700) !important;
            transform: translateY(-2px);
        }

    /* =====================================================
       PANEL LATERAL
       ===================================================== */

    .expense-form-aside {
        padding: 30px;
        border: 1px solid var(--expense-form-border);
        border-radius: 20px;
        background: radial-gradient( circle at top right, rgba(104, 165, 216, .13), transparent 35% ), var(--expense-form-white);
        box-shadow: var(--expense-form-shadow-sm);
    }

    .expense-form-aside-number {
        margin-bottom: 20px;
        color: var(--expense-form-blue-100);
        font-size: 5.2rem;
        font-weight: 800;
        letter-spacing: -.08em;
        line-height: .85;
    }

    .expense-form-aside h3 {
        margin-bottom: 12px;
        color: var(--expense-form-blue-950);
        font-size: 1.2rem;
        font-weight: 800;
    }

    .expense-form-aside p {
        margin-bottom: 0;
        color: var(--expense-form-muted);
        font-size: .88rem;
        line-height: 1.7;
    }

    /* =====================================================
       RESPONSIVE
       ===================================================== */

    @@media (max-width: 991.98px) {
        .expense-form-header {
            min-height: 290px;
            padding: 36px 32px;
        }

        .expense-form-shell {
            grid-template-columns: 1fr;
        }

        .expense-form-aside {
            order: -1;
        }
    }

    @@media (max-width: 767.98px) {
        .expense-form-grid {
            grid-template-columns: 1fr;
        }

        .expense-form-field.full-width {
            grid-column: auto;
        }
    }

    @@media (max-width: 575.98px) {
        .expense-form-header {
            min-height: 320px;
            padding: 30px 22px;
            border-radius: 18px;
        }

        .expense-form-heading h1 {
            font-size: 2.4rem;
        }

        .expense-form-panel,
        .expense-form-aside {
            padding: 24px 20px;
            border-radius: 16px;
        }

        .expense-form-actions {
            flex-direction: column;
        }

        .expense-form-submit,
        .expense-form-cancel {
            width: 100%;
        }
    }
</style>

<div class="expense-form-page">

    <!-- =====================================================
         ENCABEZADO
         ===================================================== -->
    <section class="expense-form-header">

        <div class="expense-form-heading">

            <span class="expense-form-eyebrow">
                Gestión contable
            </span>

            <h1>
                Registrar gasto
            </h1>

            <p>
                Ingresa el monto, la descripción, el empleado
                y la fecha del egreso operativo.
            </p>

        </div>

    </section>

    <!-- =====================================================
         ALERTAS
         ===================================================== -->
    <div class="expense-form-alerts">

        @if (TempData["MensajeError"] != null)
        {
            <div class="alert alert-danger alert-dismissible fade show"
                 role="alert">

                <i class="bi bi-exclamation-triangle-fill me-2"></i>

                @TempData["MensajeError"]

                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="alert"></button>

            </div>
        }

    </div>

    <!-- =====================================================
         FORMULARIO
         ===================================================== -->
    <div class="expense-form-shell">

        <section class="expense-form-panel">

            <div class="expense-form-panel-header">

                <h2>
                    Información del egreso
                </h2>

                <p>
                    Complete todos los datos requeridos antes de guardar.
                </p>

            </div>

            @using (Html.BeginForm("Agregar", "Egresos", FormMethod.Post))
            {
                @Html.AntiForgeryToken()

                <div class="expense-form-grid">

                    <div class="expense-form-field full-width">

                        @Html.LabelFor(
                            m => m.Monto,
                            new { @class = "form-label" })

                        @Html.TextBoxFor(
                            m => m.Monto,
                            new
                            {
                                @class = "form-control",
                                type = "number",
                                step = "0.01",
                                min = "0.01",
                                placeholder = "Ingrese el monto del gasto"
                            })

                        @Html.ValidationMessageFor(
                            m => m.Monto,
                            "",
                            new { @class = "text-danger small" })

                    </div>

                    <div class="expense-form-field full-width">

                        @Html.LabelFor(
                            m => m.Descripcion,
                            new { @class = "form-label" })

                        @Html.TextAreaFor(
                            m => m.Descripcion,
                            new
                            {
                                @class = "form-control",
                                rows = 3,
                                placeholder = "Describa el gasto..."
                            })

                        @Html.ValidationMessageFor(
                            m => m.Descripcion,
                            "",
                            new { @class = "text-danger small" })

                    </div>

                    <div class="expense-form-field">

                        @Html.LabelFor(
                            m => m.IdEmpleado,
                            new { @class = "form-label" })

                        @Html.DropDownListFor(
                            m => m.IdEmpleado,
                            new SelectList(
                                Model.Empleados
                                ?? new List<MiPrimeraSolucionJMKK.Abstracciones.Modelos.Contabilidad.EmpleadoDto>(),
                                "IdEmpleado",
                                "NombreCompleto"
                            ),
                            "-- Seleccione empleado --",
                            new { @class = "form-select" }
                        )

                        @Html.ValidationMessageFor(
                            m => m.IdEmpleado,
                            "",
                            new { @class = "text-danger small" })

                    </div>

                    <div class="expense-form-field">

                        @Html.LabelFor(
                            m => m.Fecha,
                            new { @class = "form-label" })

                        @Html.TextBoxFor(
                            m => m.Fecha,
                            "{0:yyyy-MM-dd}",
                            new
                            {
                                @class = "form-control",
                                type = "date"
                            })

                        @Html.ValidationMessageFor(
                            m => m.Fecha,
                            "",
                            new { @class = "text-danger small" })

                    </div>

                </div>

                <div class="expense-form-actions">

                    @Html.ActionLink(
                        "Cancelar",
                        "Index",
                        "Egresos",
                        null,
                        new
                        {
                            @class = "btn btn-secondary expense-form-cancel"
                        })

                    <button type="submit"
                            class="btn btn-pubrock expense-form-submit">

                        <i class="bi bi-check-circle-fill me-1"></i>

                        Guardar

                    </button>

                </div>
            }

        </section>

        <aside class="expense-form-aside">

            <div class="expense-form-aside-number">
                01
            </div>

            <h3>
                Registro financiero claro
            </h3>

            <p>
                Revisa el monto, la descripción, el empleado responsable
                y la fecha antes de guardar el egreso.
            </p>

        </aside>

    </div>

</div>