# Copilot Instructions

## Directrices del proyecto
- Project uses .NET Framework 4.8.1; do NOT migrate to ASP.NET Core, do NOT add MediatR or CQRS; keep existing architecture, namespaces, and folder structure; avoid adding NuGet packages unless strictly necessary; reuse existing classes/entities; align C# entities exactly with provided SQL script; stop and report if database lacks required information.